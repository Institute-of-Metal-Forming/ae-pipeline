# Data Directory

This directory will contain the prepared datasets as `*.parquet` files generated from the AA Data.
A corresponding `*.txt` file is generated containing some information about the dataset for human readers.
